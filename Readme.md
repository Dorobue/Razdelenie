# ⏱️ Анализ времени выполнения алгоритмов разбиения массива

Проект посвящён **экспериментальному сравнению алгоритмов разбиения числового массива на группы** с минимизацией дисперсии и анализу их **временной и вычислительной сложности** с использованием стандартной библиотеки `time` в Python.

Основная цель — на практике показать, как разные подходы масштабируются при увеличении размера входных данных и почему **алгоритмы полного перебора быстро становятся вычислительно и по памяти неосуществимыми**.

---

## 🧠 Постановка задачи

Дан список целых чисел.  
Требуется разделить его **на фиксированное количество подряд идущих групп** так, чтобы дисперсия сумм групп была минимальной.

Для оценки качества разбиения используется функционал:

- **Математическое ожидание**
- **Дисперсия сумм групп**

Рассматриваются три подхода:

1. **Случайное (стохастическое) разбиение**
2. **Полный перебор с рекурсивной генерацией разбиений**
3. **Полный перебор через бинарные маски**

Для каждого подхода измеряется:
- значение дисперсии `D`,
- время выполнения алгоритма.

---

## 🧩 Реализованные функции

### 📐 Математические функции

- `M(list, funct)` — вычисление математического ожидания
- `D(list)` — вычисление дисперсии

---

### 🎲 Случайное разбиение

`razdelrandom(list, n, s=None)`

- Генерирует большое число случайных разбиений
- Выбирает разбиение с минимальной дисперсией
- Число итераций `s` зависит от количества групп:
  - `s = 10**n`, если `n ≤ 6`
  - `s = 10**6` при большем числе групп

📌 Особенности:
- Не гарантирует глобальный минимум
- Масштабируется на большие входные данные
- Работает за конечное время даже при `len(list) = 150`

---

### 🧮 Полный перебор (рекурсивный)

`razdel_plus(list, n)`

- Генерирует **все возможные корректные разбиения**
- Использует рекурсивный перебор с глобальным хранилищем состояний
- Основан на построении бинарных строк-разделителей

📌 Особенности:
- Экспоненциальная сложность
- Огромное потребление памяти
- Быстро становится непригодным на практике

---

### 🔢 Полный перебор (через бинарные маски)

`razdel_plus01(list, n)`

- Перебирает бинарные маски длины `len(list)`
- Фильтрует маски с ровно `n-1` разделителем
- Формирует группы на основе маски

📌 Особенности:
- Также имеет экспоненциальную сложность
- Работает быстрее рекурсивного варианта на малых данных
- Не применим для больших массивов

---

## 📊 Экспериментальные данные

В экспериментах использовались три набора данных:

- `l0` — 10 элементов
- `l1` — 20 элементов
- `l2` — 150 элементов (случайные числа от 5 до 25)

Количество групп:
- `n = 4`
- `n = 8`

Для малых массивов тестировались **все три алгоритма**.  
Для массива из 150 элементов — **только стохастический и первый полный перебор**.

---

## 🎲 Результаты для 150 элементов и 4 групп

Входной массив:
```python
[15, 9, 17, 25, 6, 7, 22, 8, 16, 23, 6, 21, 11, 6, 7, 18, 18, 7, 12, 7,
 22, 18, 6, 23, 8, 12, 25, 25, 23, 6, 23, 23, 17, 6, 12, 6, 22, 9, 14, 18,
 9, 22, 8, 23, 14, 22, 10, 8, 23, 23, 25, 11, 16, 8, 22, 7, 23, 6, 24, 11,
 20, 22, 18, 15, 19, 23, 19, 16, 14, 12, 10, 12, 7, 23, 14, 21, 20, 15,
 19, 14, 24, 7, 8, 21, 18, 10, 15, 9, 20, 18, 6, 7, 22, 23, 15, 15, 16,
 24, 20, 23, 19, 7, 7, 13, 20, 7, 6, 14, 25, 23, 19, 14, 17, 16, 5, 19,
 16, 10, 24, 8, 20, 6, 11, 14, 9, 12, 17, 17, 20, 7, 10, 19, 17, 22,
 13, 9, 18, 22, 13, 18, 16, 17, 12, 9, 7, 10, 9, 12, 12, 5]
```
###✅ Случайное разбиение

D = 125406.0

Time ≈ 81.39 сек

Алгоритм успешно завершился

Время выполнения велико, но **конечно**.

Подходит для больших входных данных.

Качество решения зависит от *случайности*.

❌ Полный перебор (рекурсивный)
Алгоритм не завершился:


MemoryError
Фрагмент трассировки:


[Previous line repeated 137 more times]
🚨 Анализ ошибки
Ошибка MemoryError указывает на:

Экспоненциальный рост числа рекурсивных вызовов

Формирование гигантского числа промежуточных разбиений

Исчерпание доступной оперативной памяти

Даже при:

150 элементах

4 группах

полный перебор:

не масштабируется,

практически неприменим,

требует либо жёстких ограничений, либо принципиально другого подхода.

🧩 Итоговые выводы
Полный перебор имеет экспоненциальную временную и пространственную сложность

Он применим только для очень малых входных данных

Случайные и эвристические методы — единственный разумный выбор при росте размера задачи

Измерение времени через time.time() наглядно демонстрирует проблему масштабируемости

Эксперимент полностью подтверждает теоретические оценки сложности

👤 Автор
Игорь Дорофеев
Python · Алгоритмы · Анализ сложности
(контакты — в профиле GitHub)